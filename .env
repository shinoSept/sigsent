# ตั้งเป็น 1 = ใช้ข้อมูลจำลอง (mock) ทำงานได้โดยไม่ต้องมี backend
# ตั้งเป็น 0 = ยิงไปที่ backend จริงตาม EXPO_PUBLIC_API_URL ด้านล่าง
EXPO_PUBLIC_USE_MOCK=1
EXPO_PUBLIC_API_URL=http://localhost:8000
